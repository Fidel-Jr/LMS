@model LmsProject.Models.Dtos.MaterialDto
@{
    ViewData["Title"] = "New Material";
}

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

<div class="container mt-5">
    <h3>Add Material</h3>
    <div asp-validation-summary="All" class="text-danger"></div>
    <form asp-action="Create" method="post" enctype="multipart/form-data">
        <div class="mb-3">
            <label class="form-label">Title</label>
            <input id="title" asp-for="Title" class="form-control" placeholder="Title">
            <span asp-validation-for="Title" class="text-danger"></span>
        </div>
        <!-- Text area -->
        <div class="mb-3">
            <label class="form-label">Material Content</label>
            <textarea id="lessonText" asp-for="Content" class="form-control" rows="5" placeholder="Type lesson or instructions here..."></textarea>
            <span asp-validation-for="Content" class="text-danger"></span>
        </div>

        <!-- File upload -->
        <div class="mb-3">
            <label class="form-label">Upload Files (PDF/Video)</label>
            <input asp-for="Files" type="file" class="form-control"
                   accept=".pdf,.doc,.docx,.ppt,.pptx,.xls,.xlsx,.txt,.png,.jpg,.jpeg" multiple />
            <span asp-validation-for="Files" class="text-danger"></span>
        </div>

        <!-- Link input -->
        <div class="mb-3">
            <label class="form-label">Add External Link</label>
            <input type="url" id="linkInput" asp-for="ExternalLink" class="form-control" placeholder="https://example.com">
            <span asp-validation-for="ExternalLink" class="text-danger"></span>
        </div>

        <!-- Save button -->
        <button type="submit" class="btn btn-primary">Save Lesson</button>
    </form>
</div>


@* <div class="card">
    <div class="card-header bg-light">
        <!-- Toolbar -->
        <div class="btn-toolbar" role="toolbar">
            <div class="btn-group me-2">
                <button type="button" class="btn btn-sm btn-light"><i class="bi bi-type-bold"></i></button>
                <button type="button" class="btn btn-sm btn-light"><i class="bi bi-type-italic"></i></button>
                <button type="button" class="btn btn-sm btn-light"><i class="bi bi-type-underline"></i></button>
            </div>

            <div class="btn-group me-2">
                <button type="button" class="btn btn-sm btn-light"><i class="bi bi-list-ul"></i></button>
                <button type="button" class="btn btn-sm btn-light"><i class="bi bi-list-ol"></i></button>
            </div>

            <div class="btn-group me-2">
                <button type="button" class="btn btn-sm btn-light"><i class="bi bi-link-45deg"></i></button>
                <label class="btn btn-sm btn-light mb-0">
                    <i class="bi bi-paperclip"></i>
                    <input type="file" hidden>
                </label>
            </div>

            <div class="btn-group me-2">
                <label class="btn btn-sm btn-light mb-0">
                    <i class="bi bi-file-earmark-pdf"></i>
                    <input type="file" accept=".pdf" hidden>
                </label>
                <label class="btn btn-sm btn-light mb-0">
                    <i class="bi bi-file-earmark-play"></i>
                    <input type="file" accept="video/*" hidden>
                </label>
            </div>
        </div>
    </div>

    <div class="card-body">
        <!-- Textarea -->
        <textarea class="form-control" rows="6" placeholder="Type lesson or instructions here..."></textarea>
    </div>

    <div class="card-footer text-muted">
        Word count: 0
    </div>
</div> *@
